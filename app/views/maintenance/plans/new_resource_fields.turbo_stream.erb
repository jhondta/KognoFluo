<%= turbo_stream.append "#{params[:type]}-container" do %>
  <%= render partial: "resource_fields",
             locals: { index: @index, resource_type: @resource_type,
                       resource: Maintenance::PlanResource.new } %>
<% end %>
